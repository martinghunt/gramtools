sudo: require
env:
  - DIST=trusty
  - STACK=EC2
script:
  - source ~/virtualenv/python3.6/bin/activate
  - python --version
  - sudo pip install numpy
  - sudo pip install biopython
  - sudo pip install pytest
  - (cd gramtools/ && python -m pytest)
notifications:
  email: false