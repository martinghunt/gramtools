dist: trusty
sudo: require
language: python
python:
  - 3.6
script:
  - source ~/virtualenv/python3.6/bin/activate
  - python --version
  - sudo python -m pip install numpy
  - sudo python -m pip install biopython
  - sudo python -m pip install pytest
  - (cd gramtools/ && python -m pytest)
notifications:
  email: false